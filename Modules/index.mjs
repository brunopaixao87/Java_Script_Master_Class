/**
 * No ES6, ou ECMAScript 2015, foi especificado na 
 * própria linguagem, baseado no conceito de importação
 * e exportação
 */
import Circle from './circle'

 const circle = new Circle(10);
 console.log(circle);
 console.log(circle.area);